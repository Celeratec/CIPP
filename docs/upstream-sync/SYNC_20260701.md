# Upstream Sync Checkpoint — July 1, 2026 (Light Delta)

Cycle type: **Light delta** per [PROCESS.md](PROCESS.md).
Baseline moved: CIPP **10.5.2 → 10.5.5**, CIPP-API **10.5.2 → 10.5.6**.

- CIPP: 42 commits applied on `manage365/upstream-sync-cipp-20260701` (backup tag `backup/pre-upstream-sync-cipp-20260701`)
- CIPP-API: 56 commits applied on `manage365/upstream-sync-cipp-api-20260701` (backup tag `backup/pre-upstream-sync-cipp-api-20260701`)

## Verification

- `yarn build` (Node 22.13.0): pass
- PowerShell parser on 81 changed `.ps1/.psd1` files: 0 errors
- JSON validation on all changed `.json` files: pass
- No stray conflict markers

## Taken (highlights)

### CIPP-API (10.5.2 → 10.5.6)

- **Offboarding request validation** — new `Test-CIPPOffboardingRequest` rejects malformed payloads with 400 instead of silently no-oping. Extended to also accept fork-style `{ userPrincipalName }` user objects.
- **User-agent / task / standard contexts** — `Get-CippUserAgent`, `Set-CippUserAgentContext` for attributable Graph request user agents; fixes standards/scheduled-task logging.
- **Spam filter policy custom-name fixes** — matches legacy/default policy names only when no custom name is set; skips rule remediation for the built-in `Default` policy.
- **Email OTP for B2B guests** — `Set-CIPPAuthenticationPolicy` now supports `allowExternalIdToUseEmailOtp` and group exclusions (standard file itself not taken — fork does not have the new single AuthenticationMethods standard).
- **Intune policy clone + edit name/description endpoints** (`AddIntunePolicyClone`, improved `EditIntunePolicy` with odata-type normalization and settings-catalog `name` handling).
- **Mailbox rule removal hardening** for duplicate aliases (`MailboxObjectId` passthrough).
- **Excluded licenses**: `IncludeExcluded` list parameter + `ShowInLicenseDropdown` property.
- **Custom Test Engine CLM runtime**, ZTNA test expansion, run-standard logging fixes.
- **Permission repair improvements**, API client RBAC checks before creation, GDAP role mapping repair.
- Misc: New Licence updates, password generator wordlist cleanup, nextLink validation, release-notes owner/repo hardcode, NinjaOne sync moved to Reporting DB, table cleanup fixes, duplicate Intune standards fixes, CA template deployment logging, blank value fixes, EXO request fixes (incl. reverts), SharePoint Multi-Geo quota fix, Secure Score control update scoping.

### CIPP (10.5.2 → 10.5.5)

- **Timestamp parsing** — new `parseCippDate` util treats epoch-seconds values correctly in `CippTimeAgo`, `CippDataTable` sorting, and `getCippFormatting` (adapted into fork-protected files).
- **Intune policy actions** — Clone Policy and Edit Name & Description actions (ported in fork style into customized `CippIntunePolicyActions.jsx`).
- **Excluded licenses** — `IncludeExcluded` in license selectors; show/hide-in-dropdown actions on Excluded Licenses page.
- **ISO 639-1 / 3166-1 autocompletes** — new `LanguageCodeMultiSelect` / `CountryCodeMultiSelect` form types for spam filter block lists (adapted; fork keeps its own autoComplete case, new types recurse into it).
- **Contact fixes** — template creation/editing, edit page phone serialization + explicit-null clearing, `preserveNullValues` support in `CippFormPage`.
- **49GB shared mailbox conversion warning** in offboarding wizard (surgically ported into fork's customized wizard).
- **ListUsers → ListGraphRequest** migration for autopilot devices, business voice, room lists (fork's OneDrive page already migrated).
- **Teams default visibility now Private**, hideBulk on compliance actions, CA import/deploy drawer fixes, dynamic group nesting, phishing-resistant MFA card fix, permission repair UI, GDAP repair action, colliding query keys fix, "Selected Deviations" label, bookmark cleanup, alerts/M365Licenses/CIPPDBCacheTypes/standards.json data updates, `userType` column on Users list, `encodeURIComponent` on version alert query.

## Deferred (revisit next cycle)

### CIPP-API

| Item | Reason |
|------|--------|
| Purview DLP Policy standard (`a9b3e404e`) | Large feature; conflicts with fork DLP files — needs dedicated intake |
| Custom Test Alerting overhaul (`0c3d4ba7e`, `05f5f1fce`) | Conflicts in ExecTestRun/custom scripts; test after CLM runtime settles |
| Intune Policy sync / device app collection (`a2dec4281`, `10f24789c`) | Conflicts; depends on Intune report-export orchestrator not in fork |
| IP lookup + Audit Log improvements (`eac806c72`) | Conflicts with fork audit log processing |
| 90-day cache cleanup (`739d3785d`), RG name resolution (`f690b6df4`) | Conflicts; low urgency |
| APISpec move to Config (`b560d158b`), stats props (`a938c36bf`), allowed modules (`5153adc62`) | Structural; fork layout differs |
| MTD role standard (`a8143b4b2` API side + standards.json) | standards.json conflict — pair with frontend intake |
| Schema-backed standards repair (`2c63d3174`), drift logging (`ee92936ef`), sensitivity label fixes (`5392acf40`), SPO version cleanup job (`151ef4649`), github template id sync (`d2655de84`), contact template deployment (`45b96d6af`), ListLicensesReport (`2f02befcd`), requirements clarification (`7dea7f40f`), write-information fix (`07ba3d02b`), files check (`484bff838`), App Insights toggle (`04b3e124a`), permissions removal (`116429034`), json-from-frontend (`e2b092bdd`), move file (`b75c1fe90`), timer start earlier (`d2e88c5a9`) | Conflicts with fork files or depend on deferred features |

### CIPP

| Item | Reason |
|------|--------|
| PrivateRoute/login tweaks (`539ef35d3`, `a79f4100e`, `5e8a4d941`, `e9e0147c4`) | Fork has custom fast-path PrivateRoute + unauthenticated page; revisit with SSO intake |
| SPO version cleanup UI (`5426c48b0`, `ca78e05c8`) | Backend pair deferred |
| Tenant metrics grid responsive style (`495f38808`) | Fork intentionally uses compact style |
| Build optimisations (`907075a91`) | Whole-tree refactor (standards-data, license catalog utils); fork-protected files |
| Copilot menu + pages (`cd773184c` / API `114f3c7f5`, `4160b5160`, `83c77358f`, `0a3409a5f`) | New feature area — schedule as feature intake |
| CIPP Users page (`cacd07688`), user auth/sync logic (API `920e3a1c3`), Initialize-CIPPAuth (`3cd0b3f5c`, `00597e40a`), SSO password policy (`d2e54ee85`), redirect uri patch (`504c21ae3`) | SSO/CIPP-user feature family — part of planned SSO intake |
| Container management (`024c3daaa`, `c12962d60`, `cc7e46839`) | Fork does not use container management |
| Dependency bumps (react-query-devtools, mui-tiptap, apexcharts, reduxjs/toolkit, dompurify, blob upload action) | Take deliberately with regression testing, not via cherry-pick |

## Fork customizations verified intact

- `CippDataTable` mobile card view (timestamp fix applied surgically)
- Custom offboarding wizard with risk coaching (49GB warning added inside it)
- Custom users index (mobile-aware; userType column added)
- Custom contacts edit page (Container layout preserved)
- `CippFormComponent` autoComplete row-placeholder substitution (new ISO types recurse into it)
- Custom PrivateRoute fast path, custom top-nav (bookmark hook cleanup applied; top-nav untouched)
- Fork error-guidance pattern in quarantine/guest-invite flows (untouched this cycle)
