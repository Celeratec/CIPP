# Upstream Sync Checkpoint — July 13, 2026 (Major Delta)

Cycle type: **Major delta** per [PROCESS.md](PROCESS.md) — upstream shipped a full release cycle
(10.5.x → **10.6.1**, 187 API commits / 156 frontend commits inventoried, plus follow-ups).

Baseline moved: CIPP **10.5.5 → 10.6.1**, CIPP-API **10.5.6 → 10.6.1**.

- CIPP: 76 commits applied on `manage365/upstream-sync-cipp-20260713` (backup tag `backup/pre-upstream-sync-cipp-20260713`)
- CIPP-API: 119 commits applied on `manage365/upstream-sync-cipp-api-20260713` (backup tag `backup/pre-upstream-sync-cipp-api-20260713`)

## Verification

- `yarn build` (Node 22.13.0): pass
- PowerShell parser on 248 changed `.ps1/.psm1` files: 0 errors
- JSON validation on all changed `.json` files: pass
- No stray conflict markers
- iCloud sync note: `package.json` / `yarn.lock` were evicted mid-cycle by iCloud and
  accidentally committed as deletions; restored in `04354f5de`. **Move the repos out of
  `~/Documents` or disable iCloud sync for this folder.**

## Taken (highlights)

### CIPP-API (10.5.6 → 10.6.1)

- **Audit log V2 planner** — full rework for resilience: `Start-AuditLogPlannerV2` timers
  (replaces old orchestrator entries in `Config/CIPPTimers.json`; fork processor queue timer kept),
  manual search fixes, log-collection payload updates.
- **Direction-scoped Intune group assignment** — `Set-CIPPAssignedPolicy` /
  `Set-CIPPAssignedApplication` accept `ExcludeGroupIds`/`ExcludeGroupNames` and an assignment
  direction so Replace can edit one direction without wiping the other; square-bracket escaping in
  group name matching.
- **Clearable fields in user edit** — `Invoke-EditUser` explicit `clearProperties` whitelist,
  adapted into the fork's hashtable-based `$BodyToShip` (nulls for scalars, empty arrays for collections).
- **Group member handling** — enhanced add/remove logging with group names (fork's
  already-a-member tolerance preserved); sec-group add fix; mail-enabled security group removal
  during license removal (fork's actionable error messages preserved).
- **Auth method sub-settings** — `Set-CIPPAuthenticationPolicy` supports SMS/Voice/QRCodePin
  sub-settings; upstream Email OTP implementation absorbed (already carried fork's B2B params).
- **SMTP client auth state** in `Invoke-ListUserMailboxDetails` (fork's BlockExternalInbound/
  Outbound fields preserved; AutoExpandingArchiveScope skipped).
- **Drift management** — stale-entry pruning, valid-standard tracking, package-map CA tag lookup,
  template depth fix, deleted-template row cleanup.
- **CIPPSharp** aligned with upstream master (source + binary; no fork customizations existed).
- **Permission/RBAC hardening** — `Get-CippHttpPermissions`, role dedupe scoping, `.None`
  exclusion, base-name permission matching, upstream's permission-repair reverts.
- **Group templates**: email aliases + hide-from-GAL support.
- Misc: EXO error preservation, JSON escaping for Intune policies, Graph POST retry-reason
  logging, table read retry, AzBobbyTables bump, token timer secret cleanup, extension alert
  watermark fix, DMARC onmicrosoft skip, BEC message trace, incident severity endpoint, Rogue App
  List, ACSC Essential Eight test suite, test runner/scaffolder, OpenAPI enrichment, backup
  replication, vacation-mode/offboarding fixes, curated `standards.json` refresh (upstream tip
  minus standards without fork implementations).

### CIPP (10.5.5 → 10.6.1)

- **Audit log search UI** — upstream's rewritten searches page (V2 pipeline pairing), manual
  searches restored, tabOptions update; legacy coverage page removed with upstream.
- **Intune assignment pickers** — include/exclude group pickers + assignment direction radio on
  policy, script, and application assign actions (fork's color-coded action categories preserved);
  per-field `condition` support in `CippApiDialog` (auto-merged).
- **User template switching fix** — template-driven fields are replaced/cleared on switch instead
  of lingering (adapted into fork's FormSection-styled `CippAddEditUser`); array-notation +
  business phone template fixes; `calculatedGroupType` passed on group add/remove options.
- **Clearable profile fields** — edit-user page reports actively-emptied fields via
  `clearProperties` (dirty-field tracking with `useFormState`), pairing with the API change.
- **JIT admin roles** — dedicated `JitAdminRoles.json` (replaces GDAP catalog reuse), current
  Entra role names, Microsoft 365 / Entra Backup roles added.
- **Entra terminology sweep** — Azure AD → Microsoft Entra in UI labels, GDAP role catalog,
  device trust/join state rendering, managedDevice translation keys; Compliance Portal → Purview
  Portal renames.
- **Backup replication card** + `ReplicationScopeCard` on settings/backup.
- **Group templates**: email aliases + hide-from-GAL fields integrated into fork's redesigned
  add-group form (distribution + mail-enabled security).
- **Alerts/PSA UX family** — PSA Ticket Strategy on alert form, HaloPSA integration clarity,
  helperText styling, audit alert presets for external forwarding/Exchange admin elevation.
- **Reports** — shared mailbox enabled report with allTenants + ReportDB, HVEAccounts cache type,
  incident Set Severity + resolving comment, vacation mode task details, drift management
  improvements, app registration improvements, delivery insights + paste input mode.
- Misc: `util-columnsFromAPI` refactor (fork's skipRecursion + prototype-pollution guard
  reapplied), TabbedLayout advanced-tab gating, room calendar processing options, default calendar
  permission warning, CippAutocomplete tenant-scoped query keys, bulk-action row counts, CA
  session-control editing fix, stale Entra devices remediation action, standards/alerts JSON
  refreshes.

## Deferred (revisit next cycle / feature intake)

Carried forward from July 1 (see [SYNC_20260701.md](SYNC_20260701.md)) plus new this cycle:

### CIPP-API

| Item | Reason |
|------|--------|
| SAM certificate auth family (`da30ffcad`, `bf76f2bd6`, `2e636ca54`, `5f2a3e3ec`, `2b3ec21dd`, `e7ed3a226`, `6c86428cc`, `82ac60ee9`) | Foundational auth change — dedicated feature intake with staged rollout |
| SharePoint full site management suite (`d50b71747`, `0fe9645a1`, `2bc15bf02`, `2e8531cf1`, `06d0bd80f`, `559036d6f`, `aa7e060b6`) | Depends on SAM cert auth; fork has protected non-group member management |
| Copilot feature area (`114f3c7f5`, `4160b5160`, `83c77358f`, `0a3409a5f`) | New feature area — schedule as feature intake |
| SSO / CIPP user family (`920e3a1c3`, `3cd0b3f5c`, `00597e40a`, `d2e54ee85`, `504c21ae3`) | Planned SSO intake (phases already scoped) |
| `915355cb9` Set-CIPPUser refactor of EditUser | Fork's custom `Invoke-EditUser.ps1`; needed for scheduled user *edits* — pair with next cycle |
| Purview DLP standard, Custom Test Alerting overhaul, Intune policy sync, IP lookup/audit improvements, cache cleanup, RG resolution, APISpec move, MTD role standard, schema-backed standards repair, sensitivity label color family, Sherweb API client fix | Same reasons as July 1 checkpoint |

### CIPP

| Item | Reason |
|------|--------|
| Scheduled user *edit* UI (`506d75ab2`) | Requires API `Set-CIPPUser` refactor (deferred above) |
| BEC UI family (`c1fb0f92a`, `cb81f1a07`, `a445e3981`, `8359da60e`, `488541e0a`) | Fork has 6 custom BEC commits incl. retry logic — adapt as a unit |
| Dashboard Alerts card (`3db7206d0`) | Fork dashboard has 18 custom commits; API endpoint (`93ea8ca12`) already taken |
| SharePoint UI family (deleted sites, external users, edit site properties, sharing report) | API dependencies on SAM cert auth |
| Exchange/mailbox page family (`ed025994c`, `6433cd3a2`, `fc446f8d0`, `553e4cfc2`, `a3d25bfbb`, `605990cdb`, `b1838ab84`, `afca350cb`) | Fork has 11–15 custom commits on these files — adapt as a unit |
| UserActions family (`01cff98d1`, `af1fe13ff`, `22f153f48`, `f66e74a07`) | Fork has 18 custom commits on `CippUserActions.jsx` |
| Nested group memberships (`456662835`, `1e60e5f2b`) | Fork `groups/edit.jsx` has group-based licensing customization |
| MEM devices page (`35a019f65`, `b6d2de4d7`, `eb1935152`), get-cipp-formatting (`9fd979ec9`), TableToptoolbar (`c4608b26c`), ChartCard (`690f7720a`), ApiDialog (`f543ba8cb`), config.js menu changes (`c38010f5e`, `2d50bf858`, `612ef72b8`), top-nav (`2d0b8f271`), DataTable (`0a3fe87d2`, `04d89c427`), ApiResults (`0fc6459db`) | Protected/heavily customized fork files — selective adaptation only |
| Shadow AI report button removal (`6a1d7de99`), coverage page removal already taken | — |
| PrivateRoute/login tweaks, SPO version cleanup UI, tenant metrics grid style, build optimisations, Copilot pages, CIPP Users page, container management, dependency bumps | Same reasons as July 1 checkpoint |

## Fork customizations verified intact

- `CippDataTable` mobile card view; custom users index; custom contacts edit page
- Custom offboarding wizard with risk coaching
- `CippFormComponent` autoComplete row-placeholder substitution
- Custom PrivateRoute fast path and top-nav
- Color-coded action categories (preserved through every adapted actions file)
- SharePoint non-group member management + guest `#EXT#` handling
- Fork error-guidance pattern (`CippApiResults` classification)
- Curated `standards.json` (upstream standards without fork implementations excluded)
